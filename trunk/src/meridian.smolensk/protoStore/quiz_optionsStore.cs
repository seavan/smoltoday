/* Automatically generated codefile, Meridian
 * Generated by magic, please do not interfere
 * Please sleep well and do not smoke. Love, Sam */

using System;
using System.Linq;
using System.Text;
using System.Data.Linq;
using System.Collections.Generic;
using MySql.Data.MySqlClient;
using meridian.smolensk;
using meridian.smolensk.system;

namespace meridian.smolensk.protoStore
{
	public partial class quiz_optionsStore : Meridian.IEntityStore, admin.db.IDataService<proto.quiz_options>
	{
		public quiz_optionsStore()
		{
			m_Items = new SortedList<long, proto.quiz_options>();
		}
		private SortedList<long, proto.quiz_options> m_Items;
		public void Commit()
		{
			//throw new NotImplementedException();
		}
		public IQueryable<proto.quiz_options> GetAll()
		{
			return Queryable.AsQueryable<proto.quiz_options>(All());
		}
		public proto.quiz_options GetById(long id)
		{
			return Get(id);
		}
		void admin.db.IDataService<proto.quiz_options>.Insert(proto.quiz_options item)
		{
			Insert(item);
		}
		void admin.db.IDataService<proto.quiz_options>.Delete(proto.quiz_options item)
		{
			Delete(item);
		}
		public proto.quiz_options CreateItem()
		{
			return new proto.quiz_options() {   };
		}
		public void AbortItem(proto.quiz_options item)
		{
			Delete(item);
		}
		public void Discard()
		{
			;
		}
		void admin.db.IDataService<proto.quiz_options>.Update(proto.quiz_options item)
		{
			Persist(item);
		}
		public object GetObject(long _id)
		{
			return Get(_id);
		}
		public System.Type GetObjectType()
		{
			return typeof(proto.quiz_options);
		}
		public void DeleteById(long _id)
		{
			Delete(Get(_id));
		}
		public void UpdateById(long _id)
		{
			Update(Get(_id));
		}
		public void LoadAggregations(Meridian _meridian)
		{
			foreach(var item in m_Items.Values)
			{
				item.LoadAggregations(_meridian);
			}
		}
		public void Select(MySqlConnection _connection)
		{
			var cmd = new MySqlCommand("SELECT `id`, `quiz_id`, `value`, `order` FROM quiz_options");
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.quiz_options();
					item.LoadFromReader(reader);
					m_Items[item.id] = item;
				}
			}
		}
		public proto.quiz_options Insert(MySqlConnection _connection, proto.quiz_options _item)
		{
			var cmd = new MySqlCommand("INSERT INTO quiz_options ( `quiz_id`, `value`, `order` ) VALUES ( @quiz_id, @value, @order ); SELECT LAST_INSERT_ID();"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "quiz_id", Value = _item.quiz_id });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "value", Value = _item.value });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "order", Value = _item.order });
			cmd.Connection = _connection;
			_item.id = Convert.ToInt64(cmd.ExecuteScalar());
			m_Items.Add(_item.id, _item);
			_item.LoadAggregations(Meridian.Default);
			return _item;
		}
		public proto.quiz_options Update(MySqlConnection _connection, proto.quiz_options _item)
		{
			bool changed =  false;
			var cmdText =  "";
			var cmd = new MySqlCommand("UPDATE quiz_options SET `quiz_id`= @quiz_id, `value`= @value, `order`= @order WHERE id=@id"); ;
			if(_item.mc_id)
			{
			}
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			if(_item.mc_quiz_id)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "quiz_id = @quiz_id";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "quiz_id", Value = _item.quiz_id });
			}
			if(_item.mc_value)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "value = @value";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "value", Value = _item.value != null ? (object)_item.value : DBNull.Value });
			}
			if(_item.mc_order)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "order = @order";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "order", Value = _item.order });
			}
			if(changed)
			{
				cmd.CommandText =  "UPDATE quiz_options SET " + cmdText + " WHERE id=@id";
				cmd.Connection = _connection;
				cmd.ExecuteNonQuery();
				_item.LoadAggregations(Meridian.Default);
			}
			return _item;
		}
		public proto.quiz_options Delete(MySqlConnection _connection, proto.quiz_options _item)
		{
			var cmd = new MySqlCommand("DELETE FROM quiz_options WHERE id=@id"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			cmd.Connection = _connection;
			cmd.ExecuteScalar();
			return _item;
		}
		public proto.quiz_options Insert(proto.quiz_options _item)
		{
			MeridianMonitor.Default.MySqlActionForeground((conn) => Insert(conn, _item));;
			return _item;
		}
		public proto.quiz_options Persist(proto.quiz_options _item)
		{
			if(_item.id <= 0)
			{
				_item = Insert(_item);
			}
			_item = Update(_item);
			return _item;
		}
		public proto.quiz_options Delete(proto.quiz_options _item)
		{
			_item.DeleteCompositions(Meridian.Default);
			_item.DeleteAggregations();
			m_Items.Remove(_item.id);
			MeridianMonitor.Default.MySqlActionBackground((conn) => Delete(conn, _item));;
			return _item;
		}
		public proto.quiz_options Update(proto.quiz_options _item)
		{
			MeridianMonitor.Default.MySqlActionBackground((conn) => Update(conn, _item));;
			return _item;
		}
		public IList<proto.quiz_options> All()
		{
			return m_Items.Values;
		}
		public proto.quiz_options Get(long _id)
		{
			return m_Items[_id];
		}
		public bool Exists(long _id)
		{
			return m_Items.ContainsKey(_id);
		}
		public void UpdateSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			var cmd = new MySqlCommand("SELECT `id`, `quiz_id`, `value`, `order` FROM quiz_options WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					item.DeleteCompositions(Meridian.Default);
					item.DeleteAggregations();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
				}
			}
		}
		public void InsertSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if(Exists(_id)) return;;
			var cmd = new MySqlCommand("SELECT `id`, `quiz_id`, `value`, `order` FROM quiz_options WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.quiz_options();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
					m_Items.Add(item.id, item);
				}
			}
		}
		public void DeleteSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			item.DeleteCompositions(Meridian.Default);
			item.DeleteAggregations();
			m_Items.Remove(item.id);
		}
	}
}
