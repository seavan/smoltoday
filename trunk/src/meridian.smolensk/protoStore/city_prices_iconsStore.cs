/* Automatically generated codefile, Meridian
 * Generated by magic, please do not interfere
 * Please sleep well and do not smoke. Love, Sam */

using System;
using System.Linq;
using System.Text;
using System.Data.Linq;
using System.Collections.Generic;
using MySql.Data.MySqlClient;
using meridian.smolensk;
using meridian.smolensk.system;

namespace meridian.smolensk.protoStore
{
	public partial class city_prices_iconsStore : Meridian.IEntityStore, admin.db.IDataService<proto.city_prices_icons>
	{
		public city_prices_iconsStore()
		{
			m_Items = new SortedList<long, proto.city_prices_icons>();
		}
		private SortedList<long, proto.city_prices_icons> m_Items;
		public void Commit()
		{
			//throw new NotImplementedException();
		}
		public IQueryable<proto.city_prices_icons> GetAll()
		{
			return Queryable.AsQueryable<proto.city_prices_icons>(All());
		}
		public proto.city_prices_icons GetById(long id)
		{
			return Get(id);
		}
		void admin.db.IDataService<proto.city_prices_icons>.Insert(proto.city_prices_icons item)
		{
			Insert(item);
		}
		void admin.db.IDataService<proto.city_prices_icons>.Delete(proto.city_prices_icons item)
		{
			Delete(item);
		}
		public proto.city_prices_icons CreateItem()
		{
			return new proto.city_prices_icons() {   };
		}
		public void AbortItem(proto.city_prices_icons item)
		{
			Delete(item);
		}
		public void Discard()
		{
			;
		}
		void admin.db.IDataService<proto.city_prices_icons>.Update(proto.city_prices_icons item)
		{
			Persist(item);
		}
		public object GetObject(long _id)
		{
			return Get(_id);
		}
		public System.Type GetObjectType()
		{
			return typeof(proto.city_prices_icons);
		}
		public void DeleteById(long _id)
		{
			Delete(Get(_id));
		}
		public void UpdateById(long _id)
		{
			Update(Get(_id));
		}
		public void LoadAggregations(Meridian _meridian)
		{
			foreach(var item in m_Items.Values)
			{
				item.LoadAggregations(_meridian);
			}
		}
		public void Select(MySqlConnection _connection)
		{
			var cmd = new MySqlCommand("SELECT `id`, `title`, `css_class` FROM city_prices_icons");
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.city_prices_icons();
					item.LoadFromReader(reader);
					m_Items[item.id] = item;
				}
			}
		}
		public proto.city_prices_icons Insert(MySqlConnection _connection, proto.city_prices_icons _item)
		{
			var cmd = new MySqlCommand("INSERT INTO city_prices_icons ( `title`, `css_class` ) VALUES ( @title, @css_class ); SELECT LAST_INSERT_ID();"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "css_class", Value = _item.css_class });
			cmd.Connection = _connection;
			_item.id = Convert.ToInt64(cmd.ExecuteScalar());
			m_Items.Add(_item.id, _item);
			_item.LoadAggregations(Meridian.Default);
			return _item;
		}
		public proto.city_prices_icons Update(MySqlConnection _connection, proto.city_prices_icons _item)
		{
			bool changed =  false;
			var cmdText =  "";
			var cmd = new MySqlCommand("UPDATE city_prices_icons SET `title`= @title, `css_class`= @css_class WHERE id=@id"); ;
			if(_item.mc_id)
			{
			}
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			if(_item.mc_title)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "title = @title";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title != null ? (object)_item.title : DBNull.Value });
			}
			if(_item.mc_css_class)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "css_class = @css_class";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "css_class", Value = _item.css_class != null ? (object)_item.css_class : DBNull.Value });
			}
			if(changed)
			{
				cmd.CommandText =  "UPDATE city_prices_icons SET " + cmdText + " WHERE id=@id";
				cmd.Connection = _connection;
				cmd.ExecuteNonQuery();
				_item.LoadAggregations(Meridian.Default);
			}
			return _item;
		}
		public proto.city_prices_icons Delete(MySqlConnection _connection, proto.city_prices_icons _item)
		{
			var cmd = new MySqlCommand("DELETE FROM city_prices_icons WHERE id=@id"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			cmd.Connection = _connection;
			cmd.ExecuteScalar();
			return _item;
		}
		public proto.city_prices_icons Insert(proto.city_prices_icons _item)
		{
			MeridianMonitor.Default.MySqlActionForeground((conn) => Insert(conn, _item));;
			return _item;
		}
		public proto.city_prices_icons Persist(proto.city_prices_icons _item)
		{
			if(_item.id <= 0)
			{
				_item = Insert(_item);
			}
			_item = Update(_item);
			return _item;
		}
		public proto.city_prices_icons Delete(proto.city_prices_icons _item)
		{
			_item.DeleteCompositions(Meridian.Default);
			_item.DeleteAggregations();
			m_Items.Remove(_item.id);
			MeridianMonitor.Default.MySqlActionBackground((conn) => Delete(conn, _item));;
			return _item;
		}
		public proto.city_prices_icons Update(proto.city_prices_icons _item)
		{
			MeridianMonitor.Default.MySqlActionBackground((conn) => Update(conn, _item));;
			return _item;
		}
		public IList<proto.city_prices_icons> All()
		{
			return m_Items.Values;
		}
		public proto.city_prices_icons Get(long _id)
		{
			return m_Items[_id];
		}
		public bool Exists(long _id)
		{
			return m_Items.ContainsKey(_id);
		}
		public void UpdateSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			var cmd = new MySqlCommand("SELECT `id`, `title`, `css_class` FROM city_prices_icons WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					item.DeleteCompositions(Meridian.Default);
					item.DeleteAggregations();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
				}
			}
		}
		public void InsertSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if(Exists(_id)) return;;
			var cmd = new MySqlCommand("SELECT `id`, `title`, `css_class` FROM city_prices_icons WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.city_prices_icons();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
					m_Items.Add(item.id, item);
				}
			}
		}
		public void DeleteSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			item.DeleteCompositions(Meridian.Default);
			item.DeleteAggregations();
			m_Items.Remove(item.id);
		}
	}
}
