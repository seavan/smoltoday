/* Automatically generated codefile, Meridian
 * Generated by magic, please do not interfere
 * Please sleep well and do not smoke. Love, Sam */

using System;
using System.Linq;
using System.Text;
using System.Data.Linq;
using System.Collections.Generic;
using MySql.Data.MySqlClient;
using meridian.smolensk;
using meridian.smolensk.system;

namespace meridian.smolensk.protoStore
{
	public partial class quizzesStore : Meridian.IEntityStore, admin.db.IDataService<proto.quizzes>
	{
		public quizzesStore()
		{
			m_Items = new SortedList<long, proto.quizzes>();
		}
		private SortedList<long, proto.quizzes> m_Items;
		public void Commit()
		{
			//throw new NotImplementedException();
		}
		public IQueryable<proto.quizzes> GetAll()
		{
			return Queryable.AsQueryable<proto.quizzes>(All());
		}
		public proto.quizzes GetById(long id)
		{
			return Get(id);
		}
		void admin.db.IDataService<proto.quizzes>.Insert(proto.quizzes item)
		{
			Insert(item);
		}
		void admin.db.IDataService<proto.quizzes>.Delete(proto.quizzes item)
		{
			Delete(item);
		}
		public proto.quizzes CreateItem()
		{
			return new proto.quizzes() {   };
		}
		public void AbortItem(proto.quizzes item)
		{
			Delete(item);
		}
		public void Discard()
		{
			;
		}
		void admin.db.IDataService<proto.quizzes>.Update(proto.quizzes item)
		{
			Persist(item);
		}
		public object GetObject(long _id)
		{
			return Get(_id);
		}
		public System.Type GetObjectType()
		{
			return typeof(proto.quizzes);
		}
		public void DeleteById(long _id)
		{
			Delete(Get(_id));
		}
		public void UpdateById(long _id)
		{
			Update(Get(_id));
		}
		public void LoadAggregations(Meridian _meridian)
		{
			foreach(var item in m_Items.Values)
			{
				item.LoadAggregations(_meridian);
			}
		}
		public void Select(MySqlConnection _connection)
		{
			var cmd = new MySqlCommand("SELECT `id`, `title`, `datetime_start`, `datetime_finish`, `datetime_publish`, `is_main` FROM quizzes");
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.quizzes();
					item.LoadFromReader(reader);
					m_Items[item.id] = item;
				}
			}
		}
		public proto.quizzes Insert(MySqlConnection _connection, proto.quizzes _item)
		{
			var cmd = new MySqlCommand("INSERT INTO quizzes ( `title`, `datetime_start`, `datetime_finish`, `datetime_publish`, `is_main` ) VALUES ( @title, @datetime_start, @datetime_finish, @datetime_publish, @is_main ); SELECT LAST_INSERT_ID();"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "datetime_start", Value = (_item.datetime_start != null && _item.datetime_start.Year > 1800) ? _item.datetime_start : new DateTime(1800, 1, 1) });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "datetime_finish", Value = (_item.datetime_finish != null && _item.datetime_finish.Year > 1800) ? _item.datetime_finish : new DateTime(1800, 1, 1) });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "datetime_publish", Value = (_item.datetime_publish != null && _item.datetime_publish.Year > 1800) ? _item.datetime_publish : new DateTime(1800, 1, 1) });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "is_main", Value = _item.is_main });
			cmd.Connection = _connection;
			_item.id = Convert.ToInt64(cmd.ExecuteScalar());
			m_Items.Add(_item.id, _item);
			_item.LoadAggregations(Meridian.Default);
			return _item;
		}
		public proto.quizzes Update(MySqlConnection _connection, proto.quizzes _item)
		{
			bool changed =  false;
			var cmdText =  "";
			var cmd = new MySqlCommand("UPDATE quizzes SET `title`= @title, `datetime_start`= @datetime_start, `datetime_finish`= @datetime_finish, `datetime_publish`= @datetime_publish, `is_main`= @is_main WHERE id=@id"); ;
			if(_item.mc_id)
			{
			}
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			if(_item.mc_title)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "title = @title";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title != null ? (object)_item.title : DBNull.Value });
			}
			if(_item.mc_datetime_start)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "datetime_start = @datetime_start";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "datetime_start", Value = _item.datetime_start });
			}
			if(_item.mc_datetime_finish)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "datetime_finish = @datetime_finish";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "datetime_finish", Value = _item.datetime_finish });
			}
			if(_item.mc_datetime_publish)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "datetime_publish = @datetime_publish";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "datetime_publish", Value = _item.datetime_publish });
			}
			if(_item.mc_is_main)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "is_main = @is_main";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "is_main", Value = _item.is_main });
			}
			if(changed)
			{
				cmd.CommandText =  "UPDATE quizzes SET " + cmdText + " WHERE id=@id";
				cmd.Connection = _connection;
				cmd.ExecuteNonQuery();
				_item.LoadAggregations(Meridian.Default);
			}
			return _item;
		}
		public proto.quizzes Delete(MySqlConnection _connection, proto.quizzes _item)
		{
			var cmd = new MySqlCommand("DELETE FROM quizzes WHERE id=@id"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			cmd.Connection = _connection;
			cmd.ExecuteScalar();
			return _item;
		}
		public proto.quizzes Insert(proto.quizzes _item)
		{
			MeridianMonitor.Default.MySqlActionForeground((conn) => Insert(conn, _item));;
			return _item;
		}
		public proto.quizzes Persist(proto.quizzes _item)
		{
			if(_item.id <= 0)
			{
				_item = Insert(_item);
			}
			_item = Update(_item);
			return _item;
		}
		public proto.quizzes Delete(proto.quizzes _item)
		{
			_item.DeleteCompositions(Meridian.Default);
			_item.DeleteAggregations();
			m_Items.Remove(_item.id);
			MeridianMonitor.Default.MySqlActionBackground((conn) => Delete(conn, _item));;
			return _item;
		}
		public proto.quizzes Update(proto.quizzes _item)
		{
			MeridianMonitor.Default.MySqlActionBackground((conn) => Update(conn, _item));;
			return _item;
		}
		public IList<proto.quizzes> All()
		{
			return m_Items.Values;
		}
		public proto.quizzes Get(long _id)
		{
			return m_Items[_id];
		}
		public bool Exists(long _id)
		{
			return m_Items.ContainsKey(_id);
		}
		public void UpdateSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			var cmd = new MySqlCommand("SELECT `id`, `title`, `datetime_start`, `datetime_finish`, `datetime_publish`, `is_main` FROM quizzes WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					item.DeleteCompositions(Meridian.Default);
					item.DeleteAggregations();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
				}
			}
		}
		public void InsertSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if(Exists(_id)) return;;
			var cmd = new MySqlCommand("SELECT `id`, `title`, `datetime_start`, `datetime_finish`, `datetime_publish`, `is_main` FROM quizzes WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.quizzes();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
					m_Items.Add(item.id, item);
				}
			}
		}
		public void DeleteSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			item.DeleteCompositions(Meridian.Default);
			item.DeleteAggregations();
			m_Items.Remove(item.id);
		}
	}
}
