/* Automatically generated codefile, Meridian
 * Generated by magic, please do not interfere
 * Please sleep well and do not smoke. Love, Sam */

using System;
using System.Linq;
using System.Text;
using System.Data.Linq;
using System.Collections.Generic;
using MySql.Data.MySqlClient;
using meridian.smolensk;
using meridian.smolensk.system;

namespace meridian.smolensk.protoStore
{
	public partial class quiz_resultsStore : Meridian.IEntityStore, admin.db.IDataService<proto.quiz_results>
	{
		public quiz_resultsStore()
		{
			m_Items = new SortedList<long, proto.quiz_results>();
		}
		private SortedList<long, proto.quiz_results> m_Items;
		public void Commit()
		{
			//throw new NotImplementedException();
		}
		public IQueryable<proto.quiz_results> GetAll()
		{
			return Queryable.AsQueryable<proto.quiz_results>(All());
		}
		public proto.quiz_results GetById(long id)
		{
			return Get(id);
		}
		void admin.db.IDataService<proto.quiz_results>.Insert(proto.quiz_results item)
		{
			Insert(item);
		}
		void admin.db.IDataService<proto.quiz_results>.Delete(proto.quiz_results item)
		{
			Delete(item);
		}
		public proto.quiz_results CreateItem()
		{
			return new proto.quiz_results() {   };
		}
		public void AbortItem(proto.quiz_results item)
		{
			Delete(item);
		}
		public void Discard()
		{
			;
		}
		void admin.db.IDataService<proto.quiz_results>.Update(proto.quiz_results item)
		{
			Persist(item);
		}
		public object GetObject(long _id)
		{
			return Get(_id);
		}
		public System.Type GetObjectType()
		{
			return typeof(proto.quiz_results);
		}
		public void DeleteById(long _id)
		{
			Delete(Get(_id));
		}
		public void UpdateById(long _id)
		{
			Update(Get(_id));
		}
		public void LoadAggregations(Meridian _meridian)
		{
			foreach(var item in m_Items.Values)
			{
				item.LoadAggregations(_meridian);
			}
		}
		public void Select(MySqlConnection _connection)
		{
			var cmd = new MySqlCommand("SELECT `id`, `account_id`, `quiz_id`, `quiz_option_id` FROM quiz_results");
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.quiz_results();
					item.LoadFromReader(reader);
					m_Items[item.id] = item;
				}
			}
		}
		public proto.quiz_results Insert(MySqlConnection _connection, proto.quiz_results _item)
		{
			var cmd = new MySqlCommand("INSERT INTO quiz_results ( `account_id`, `quiz_id`, `quiz_option_id` ) VALUES ( @account_id, @quiz_id, @quiz_option_id ); SELECT LAST_INSERT_ID();"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "account_id", Value = _item.account_id });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "quiz_id", Value = _item.quiz_id });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "quiz_option_id", Value = _item.quiz_option_id });
			cmd.Connection = _connection;
			_item.id = Convert.ToInt64(cmd.ExecuteScalar());
			m_Items.Add(_item.id, _item);
			_item.LoadAggregations(Meridian.Default);
			return _item;
		}
		public proto.quiz_results Update(MySqlConnection _connection, proto.quiz_results _item)
		{
			bool changed =  false;
			var cmdText =  "";
			var cmd = new MySqlCommand("UPDATE quiz_results SET `account_id`= @account_id, `quiz_id`= @quiz_id, `quiz_option_id`= @quiz_option_id WHERE id=@id"); ;
			if(_item.mc_id)
			{
			}
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			if(_item.mc_account_id)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "account_id = @account_id";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "account_id", Value = _item.account_id });
			}
			if(_item.mc_quiz_id)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "quiz_id = @quiz_id";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "quiz_id", Value = _item.quiz_id });
			}
			if(_item.mc_quiz_option_id)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "quiz_option_id = @quiz_option_id";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "quiz_option_id", Value = _item.quiz_option_id });
			}
			if(changed)
			{
				cmd.CommandText =  "UPDATE quiz_results SET " + cmdText + " WHERE id=@id";
				cmd.Connection = _connection;
				cmd.ExecuteNonQuery();
				_item.LoadAggregations(Meridian.Default);
			}
			return _item;
		}
		public proto.quiz_results Delete(MySqlConnection _connection, proto.quiz_results _item)
		{
			var cmd = new MySqlCommand("DELETE FROM quiz_results WHERE id=@id"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			cmd.Connection = _connection;
			cmd.ExecuteScalar();
			return _item;
		}
		public proto.quiz_results Insert(proto.quiz_results _item)
		{
			MeridianMonitor.Default.MySqlActionForeground((conn) => Insert(conn, _item));;
			return _item;
		}
		public proto.quiz_results Persist(proto.quiz_results _item)
		{
			if(_item.id <= 0)
			{
				_item = Insert(_item);
			}
			_item = Update(_item);
			return _item;
		}
		public proto.quiz_results Delete(proto.quiz_results _item)
		{
			_item.DeleteCompositions(Meridian.Default);
			_item.DeleteAggregations();
			m_Items.Remove(_item.id);
			MeridianMonitor.Default.MySqlActionBackground((conn) => Delete(conn, _item));;
			return _item;
		}
		public proto.quiz_results Update(proto.quiz_results _item)
		{
			MeridianMonitor.Default.MySqlActionBackground((conn) => Update(conn, _item));;
			return _item;
		}
		public IList<proto.quiz_results> All()
		{
			return m_Items.Values;
		}
		public proto.quiz_results Get(long _id)
		{
			return m_Items[_id];
		}
		public bool Exists(long _id)
		{
			return m_Items.ContainsKey(_id);
		}
		public void UpdateSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			var cmd = new MySqlCommand("SELECT `id`, `account_id`, `quiz_id`, `quiz_option_id` FROM quiz_results WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					item.DeleteCompositions(Meridian.Default);
					item.DeleteAggregations();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
				}
			}
		}
		public void InsertSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if(Exists(_id)) return;;
			var cmd = new MySqlCommand("SELECT `id`, `account_id`, `quiz_id`, `quiz_option_id` FROM quiz_results WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.quiz_results();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
					m_Items.Add(item.id, item);
				}
			}
		}
		public void DeleteSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			item.DeleteCompositions(Meridian.Default);
			item.DeleteAggregations();
			m_Items.Remove(item.id);
		}
	}
}
