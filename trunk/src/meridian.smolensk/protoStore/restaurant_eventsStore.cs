/* Automatically generated codefile, Meridian
 * Generated by magic, please do not interfere
 * Please sleep well and do not smoke. Love, Sam */

using System;
using System.Linq;
using System.Text;
using System.Data.Linq;
using System.Collections.Generic;
using MySql.Data.MySqlClient;
using meridian.smolensk;
using meridian.smolensk.system;

namespace meridian.smolensk.protoStore
{
	public partial class restaurant_eventsStore : Meridian.IEntityStore, admin.db.IDataService<proto.restaurant_events>
	{
		public restaurant_eventsStore()
		{
			m_Items = new SortedList<long, proto.restaurant_events>();
		}
		private SortedList<long, proto.restaurant_events> m_Items;
		public void Commit()
		{
			//throw new NotImplementedException();
		}
		public IQueryable<proto.restaurant_events> GetAll()
		{
			return Queryable.AsQueryable<proto.restaurant_events>(All());
		}
		public proto.restaurant_events GetById(long id)
		{
			return Get(id);
		}
		void admin.db.IDataService<proto.restaurant_events>.Insert(proto.restaurant_events item)
		{
			Insert(item);
		}
		void admin.db.IDataService<proto.restaurant_events>.Delete(proto.restaurant_events item)
		{
			Delete(item);
		}
		public proto.restaurant_events CreateItem()
		{
			return new proto.restaurant_events() {   };
		}
		public void AbortItem(proto.restaurant_events item)
		{
			Delete(item);
		}
		public void Discard()
		{
			;
		}
		void admin.db.IDataService<proto.restaurant_events>.Update(proto.restaurant_events item)
		{
			Persist(item);
		}
		public object GetObject(long _id)
		{
			return Get(_id);
		}
		public System.Type GetObjectType()
		{
			return typeof(proto.restaurant_events);
		}
		public void DeleteById(long _id)
		{
			Delete(Get(_id));
		}
		public void UpdateById(long _id)
		{
			Update(Get(_id));
		}
		public void LoadAggregations(Meridian _meridian)
		{
			foreach(var item in m_Items.Values)
			{
				item.LoadAggregations(_meridian);
			}
		}
		public void Select(MySqlConnection _connection)
		{
			var cmd = new MySqlCommand("SELECT `id`, `date`, `title`, `short_desc`, `restaurant_id` FROM restaurant_events");
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.restaurant_events();
					item.LoadFromReader(reader);
					m_Items[item.id] = item;
				}
			}
		}
		public proto.restaurant_events Insert(MySqlConnection _connection, proto.restaurant_events _item)
		{
			var cmd = new MySqlCommand("INSERT INTO restaurant_events ( `date`, `title`, `short_desc`, `restaurant_id` ) VALUES ( @date, @title, @short_desc, @restaurant_id ); SELECT LAST_INSERT_ID();"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "date", Value = (_item.date != null && _item.date.Year > 1800) ? _item.date : new DateTime(1800, 1, 1) });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "short_desc", Value = _item.short_desc });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "restaurant_id", Value = _item.restaurant_id });
			cmd.Connection = _connection;
			_item.id = Convert.ToInt64(cmd.ExecuteScalar());
			m_Items.Add(_item.id, _item);
			_item.LoadAggregations(Meridian.Default);
			return _item;
		}
		public proto.restaurant_events Update(MySqlConnection _connection, proto.restaurant_events _item)
		{
			bool changed =  false;
			var cmdText =  "";
			var cmd = new MySqlCommand("UPDATE restaurant_events SET `date`= @date, `title`= @title, `short_desc`= @short_desc, `restaurant_id`= @restaurant_id WHERE id=@id"); ;
			if(_item.mc_id)
			{
			}
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			if(_item.mc_date)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "date = @date";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "date", Value = _item.date });
			}
			if(_item.mc_title)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "title = @title";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title != null ? (object)_item.title : DBNull.Value });
			}
			if(_item.mc_short_desc)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "short_desc = @short_desc";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "short_desc", Value = _item.short_desc != null ? (object)_item.short_desc : DBNull.Value });
			}
			if(_item.mc_restaurant_id)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "restaurant_id = @restaurant_id";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "restaurant_id", Value = _item.restaurant_id });
			}
			if(changed)
			{
				cmd.CommandText =  "UPDATE restaurant_events SET " + cmdText + " WHERE id=@id";
				cmd.Connection = _connection;
				cmd.ExecuteNonQuery();
				_item.LoadAggregations(Meridian.Default);
			}
			return _item;
		}
		public proto.restaurant_events Delete(MySqlConnection _connection, proto.restaurant_events _item)
		{
			var cmd = new MySqlCommand("DELETE FROM restaurant_events WHERE id=@id"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			cmd.Connection = _connection;
			cmd.ExecuteScalar();
			return _item;
		}
		public proto.restaurant_events Insert(proto.restaurant_events _item)
		{
			MeridianMonitor.Default.MySqlActionForeground((conn) => Insert(conn, _item));;
			return _item;
		}
		public proto.restaurant_events Persist(proto.restaurant_events _item)
		{
			if(_item.id <= 0)
			{
				_item = Insert(_item);
			}
			_item = Update(_item);
			return _item;
		}
		public proto.restaurant_events Delete(proto.restaurant_events _item)
		{
			_item.DeleteCompositions(Meridian.Default);
			_item.DeleteAggregations();
			m_Items.Remove(_item.id);
			MeridianMonitor.Default.MySqlActionBackground((conn) => Delete(conn, _item));;
			return _item;
		}
		public proto.restaurant_events Update(proto.restaurant_events _item)
		{
			MeridianMonitor.Default.MySqlActionBackground((conn) => Update(conn, _item));;
			return _item;
		}
		public IList<proto.restaurant_events> All()
		{
			return m_Items.Values;
		}
		public proto.restaurant_events Get(long _id)
		{
			return m_Items[_id];
		}
		public bool Exists(long _id)
		{
			return m_Items.ContainsKey(_id);
		}
		public void UpdateSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			var cmd = new MySqlCommand("SELECT `id`, `date`, `title`, `short_desc`, `restaurant_id` FROM restaurant_events WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					item.DeleteCompositions(Meridian.Default);
					item.DeleteAggregations();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
				}
			}
		}
		public void InsertSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if(Exists(_id)) return;;
			var cmd = new MySqlCommand("SELECT `id`, `date`, `title`, `short_desc`, `restaurant_id` FROM restaurant_events WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.restaurant_events();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
					m_Items.Add(item.id, item);
				}
			}
		}
		public void DeleteSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			item.DeleteCompositions(Meridian.Default);
			item.DeleteAggregations();
			m_Items.Remove(item.id);
		}
	}
}
