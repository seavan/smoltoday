/* Automatically generated codefile, Meridian
 * Generated by magic, please do not interfere
 * Please sleep well and do not smoke. Love, Sam */

using System;
using System.Linq;
using System.Text;
using System.Data.Linq;
using System.Collections.Generic;
using MySql.Data.MySqlClient;
using meridian.smolensk;
using meridian.smolensk.system;

namespace meridian.smolensk.protoStore
{
	public partial class ad_field_descriptionsStore : Meridian.IEntityStore, admin.db.IDataService<proto.ad_field_descriptions>
	{
		public ad_field_descriptionsStore()
		{
			m_Items = new SortedList<long, proto.ad_field_descriptions>();
		}
		private SortedList<long, proto.ad_field_descriptions> m_Items;
		public void Commit()
		{
			//throw new NotImplementedException();
		}
		public IQueryable<proto.ad_field_descriptions> GetAll()
		{
			return Queryable.AsQueryable<proto.ad_field_descriptions>(All());
		}
		public proto.ad_field_descriptions GetById(long id)
		{
			return Get(id);
		}
		void admin.db.IDataService<proto.ad_field_descriptions>.Insert(proto.ad_field_descriptions item)
		{
			Insert(item);
		}
		void admin.db.IDataService<proto.ad_field_descriptions>.Delete(proto.ad_field_descriptions item)
		{
			Delete(item);
		}
		public proto.ad_field_descriptions CreateItem()
		{
			return new proto.ad_field_descriptions() {   };
		}
		public void AbortItem(proto.ad_field_descriptions item)
		{
			Delete(item);
		}
		public void Discard()
		{
			;
		}
		void admin.db.IDataService<proto.ad_field_descriptions>.Update(proto.ad_field_descriptions item)
		{
			Persist(item);
		}
		public object GetObject(long _id)
		{
			return Get(_id);
		}
		public System.Type GetObjectType()
		{
			return typeof(proto.ad_field_descriptions);
		}
		public void DeleteById(long _id)
		{
			Delete(Get(_id));
		}
		public void UpdateById(long _id)
		{
			Update(Get(_id));
		}
		public void LoadAggregations(Meridian _meridian)
		{
			foreach(var item in m_Items.Values)
			{
				item.LoadAggregations(_meridian);
			}
		}
		public void Select(MySqlConnection _connection)
		{
			var cmd = new MySqlCommand("SELECT `id`, `title`, `ad_category_id`, `is_multiple`, `is_anyvalue` FROM ad_field_descriptions");
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.ad_field_descriptions();
					item.LoadFromReader(reader);
					m_Items[item.id] = item;
				}
			}
		}
		public proto.ad_field_descriptions Insert(MySqlConnection _connection, proto.ad_field_descriptions _item)
		{
			var cmd = new MySqlCommand("INSERT INTO ad_field_descriptions ( `title`, `ad_category_id`, `is_multiple`, `is_anyvalue` ) VALUES ( @title, @ad_category_id, @is_multiple, @is_anyvalue ); SELECT LAST_INSERT_ID();"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "ad_category_id", Value = _item.ad_category_id });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "is_multiple", Value = _item.is_multiple });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "is_anyvalue", Value = _item.is_anyvalue });
			cmd.Connection = _connection;
			_item.id = Convert.ToInt64(cmd.ExecuteScalar());
			m_Items.Add(_item.id, _item);
			_item.LoadAggregations(Meridian.Default);
			return _item;
		}
		public proto.ad_field_descriptions Update(MySqlConnection _connection, proto.ad_field_descriptions _item)
		{
			bool changed =  false;
			var cmdText =  "";
			var cmd = new MySqlCommand("UPDATE ad_field_descriptions SET `title`= @title, `ad_category_id`= @ad_category_id, `is_multiple`= @is_multiple, `is_anyvalue`= @is_anyvalue WHERE id=@id"); ;
			if(_item.mc_id)
			{
			}
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			if(_item.mc_title)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "title = @title";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title != null ? (object)_item.title : DBNull.Value });
			}
			if(_item.mc_ad_category_id)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "ad_category_id = @ad_category_id";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "ad_category_id", Value = _item.ad_category_id });
			}
			if(_item.mc_is_multiple)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "is_multiple = @is_multiple";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "is_multiple", Value = _item.is_multiple });
			}
			if(_item.mc_is_anyvalue)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "is_anyvalue = @is_anyvalue";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "is_anyvalue", Value = _item.is_anyvalue });
			}
			if(changed)
			{
				cmd.CommandText =  "UPDATE ad_field_descriptions SET " + cmdText + " WHERE id=@id";
				cmd.Connection = _connection;
				cmd.ExecuteNonQuery();
				_item.LoadAggregations(Meridian.Default);
			}
			return _item;
		}
		public proto.ad_field_descriptions Delete(MySqlConnection _connection, proto.ad_field_descriptions _item)
		{
			var cmd = new MySqlCommand("DELETE FROM ad_field_descriptions WHERE id=@id"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			cmd.Connection = _connection;
			cmd.ExecuteScalar();
			return _item;
		}
		public proto.ad_field_descriptions Insert(proto.ad_field_descriptions _item)
		{
			MeridianMonitor.Default.MySqlActionForeground((conn) => Insert(conn, _item));;
			return _item;
		}
		public proto.ad_field_descriptions Persist(proto.ad_field_descriptions _item)
		{
			if(_item.id <= 0)
			{
				_item = Insert(_item);
			}
			_item = Update(_item);
			return _item;
		}
		public proto.ad_field_descriptions Delete(proto.ad_field_descriptions _item)
		{
			_item.DeleteCompositions(Meridian.Default);
			_item.DeleteAggregations();
			m_Items.Remove(_item.id);
			MeridianMonitor.Default.MySqlActionBackground((conn) => Delete(conn, _item));;
			return _item;
		}
		public proto.ad_field_descriptions Update(proto.ad_field_descriptions _item)
		{
			MeridianMonitor.Default.MySqlActionBackground((conn) => Update(conn, _item));;
			return _item;
		}
		public IList<proto.ad_field_descriptions> All()
		{
			return m_Items.Values;
		}
		public proto.ad_field_descriptions Get(long _id)
		{
			return m_Items[_id];
		}
		public bool Exists(long _id)
		{
			return m_Items.ContainsKey(_id);
		}
		public void UpdateSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			var cmd = new MySqlCommand("SELECT `id`, `title`, `ad_category_id`, `is_multiple`, `is_anyvalue` FROM ad_field_descriptions WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					item.DeleteCompositions(Meridian.Default);
					item.DeleteAggregations();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
				}
			}
		}
		public void InsertSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if(Exists(_id)) return;;
			var cmd = new MySqlCommand("SELECT `id`, `title`, `ad_category_id`, `is_multiple`, `is_anyvalue` FROM ad_field_descriptions WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.ad_field_descriptions();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
					m_Items.Add(item.id, item);
				}
			}
		}
		public void DeleteSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			item.DeleteCompositions(Meridian.Default);
			item.DeleteAggregations();
			m_Items.Remove(item.id);
		}
	}
}
