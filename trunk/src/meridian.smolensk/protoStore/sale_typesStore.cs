/* Automatically generated codefile, Meridian
 * Generated by magic, please do not interfere
 * Please sleep well and do not smoke. Love, Sam */

using System;
using System.Linq;
using System.Text;
using System.Data.Linq;
using System.Collections.Generic;
using MySql.Data.MySqlClient;
using meridian.smolensk;
using meridian.smolensk.system;

namespace meridian.smolensk.protoStore
{
	public partial class sale_typesStore : Meridian.IEntityStore, admin.db.IDataService<proto.sale_types>
	{
		public sale_typesStore()
		{
			m_Items = new SortedList<long, proto.sale_types>();
		}
		private SortedList<long, proto.sale_types> m_Items;
		public void Commit()
		{
			//throw new NotImplementedException();
		}
		public IQueryable<proto.sale_types> GetAll()
		{
			return Queryable.AsQueryable<proto.sale_types>(All());
		}
		public proto.sale_types GetById(long id)
		{
			return Get(id);
		}
		void admin.db.IDataService<proto.sale_types>.Insert(proto.sale_types item)
		{
			Insert(item);
		}
		void admin.db.IDataService<proto.sale_types>.Delete(proto.sale_types item)
		{
			Delete(item);
		}
		public proto.sale_types CreateItem()
		{
			return new proto.sale_types() {   };
		}
		public void AbortItem(proto.sale_types item)
		{
			Delete(item);
		}
		public void Discard()
		{
			;
		}
		void admin.db.IDataService<proto.sale_types>.Update(proto.sale_types item)
		{
			Persist(item);
		}
		public object GetObject(long _id)
		{
			return Get(_id);
		}
		public System.Type GetObjectType()
		{
			return typeof(proto.sale_types);
		}
		public void DeleteById(long _id)
		{
			Delete(Get(_id));
		}
		public void UpdateById(long _id)
		{
			Update(Get(_id));
		}
		public void LoadAggregations(Meridian _meridian)
		{
			foreach(var item in m_Items.Values)
			{
				item.LoadAggregations(_meridian);
			}
		}
		public void Select(MySqlConnection _connection)
		{
			var cmd = new MySqlCommand("SELECT `id`, `title`, `unlimited`, `order` FROM sale_types");
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.sale_types();
					item.LoadFromReader(reader);
					m_Items[item.id] = item;
				}
			}
		}
		public proto.sale_types Insert(MySqlConnection _connection, proto.sale_types _item)
		{
			var cmd = new MySqlCommand("INSERT INTO sale_types ( `title`, `unlimited`, `order` ) VALUES ( @title, @unlimited, @order ); SELECT LAST_INSERT_ID();"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "unlimited", Value = _item.unlimited });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "order", Value = _item.order });
			cmd.Connection = _connection;
			_item.id = Convert.ToInt64(cmd.ExecuteScalar());
			m_Items.Add(_item.id, _item);
			_item.LoadAggregations(Meridian.Default);
			return _item;
		}
		public proto.sale_types Update(MySqlConnection _connection, proto.sale_types _item)
		{
			bool changed =  false;
			var cmdText =  "";
			var cmd = new MySqlCommand("UPDATE sale_types SET `title`= @title, `unlimited`= @unlimited, `order`= @order WHERE id=@id"); ;
			if(_item.mc_id)
			{
			}
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			if(_item.mc_title)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "title = @title";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title != null ? (object)_item.title : DBNull.Value });
			}
			if(_item.mc_unlimited)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "unlimited = @unlimited";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "unlimited", Value = _item.unlimited });
			}
			if(_item.mc_order)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "order = @order";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "order", Value = _item.order });
			}
			if(changed)
			{
				cmd.CommandText =  "UPDATE sale_types SET " + cmdText + " WHERE id=@id";
				cmd.Connection = _connection;
				cmd.ExecuteNonQuery();
				_item.LoadAggregations(Meridian.Default);
			}
			return _item;
		}
		public proto.sale_types Delete(MySqlConnection _connection, proto.sale_types _item)
		{
			var cmd = new MySqlCommand("DELETE FROM sale_types WHERE id=@id"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			cmd.Connection = _connection;
			cmd.ExecuteScalar();
			return _item;
		}
		public proto.sale_types Insert(proto.sale_types _item)
		{
			MeridianMonitor.Default.MySqlActionForeground((conn) => Insert(conn, _item));;
			return _item;
		}
		public proto.sale_types Persist(proto.sale_types _item)
		{
			if(_item.id <= 0)
			{
				_item = Insert(_item);
			}
			_item = Update(_item);
			return _item;
		}
		public proto.sale_types Delete(proto.sale_types _item)
		{
			_item.DeleteCompositions(Meridian.Default);
			_item.DeleteAggregations();
			m_Items.Remove(_item.id);
			MeridianMonitor.Default.MySqlActionBackground((conn) => Delete(conn, _item));;
			return _item;
		}
		public proto.sale_types Update(proto.sale_types _item)
		{
			MeridianMonitor.Default.MySqlActionBackground((conn) => Update(conn, _item));;
			return _item;
		}
		public IList<proto.sale_types> All()
		{
			return m_Items.Values;
		}
		public proto.sale_types Get(long _id)
		{
			return m_Items[_id];
		}
		public bool Exists(long _id)
		{
			return m_Items.ContainsKey(_id);
		}
		public void UpdateSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			var cmd = new MySqlCommand("SELECT `id`, `title`, `unlimited`, `order` FROM sale_types WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					item.DeleteCompositions(Meridian.Default);
					item.DeleteAggregations();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
				}
			}
		}
		public void InsertSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if(Exists(_id)) return;;
			var cmd = new MySqlCommand("SELECT `id`, `title`, `unlimited`, `order` FROM sale_types WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.sale_types();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
					m_Items.Add(item.id, item);
				}
			}
		}
		public void DeleteSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			item.DeleteCompositions(Meridian.Default);
			item.DeleteAggregations();
			m_Items.Remove(item.id);
		}
	}
}
