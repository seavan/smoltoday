/* Automatically generated codefile, Meridian
 * Generated by magic, please do not interfere
 * Please sleep well and do not smoke. Love, Sam */

using System;
using System.Linq;
using System.Text;
using System.Data.Linq;
using System.Collections.Generic;
using MySql.Data.MySqlClient;
using meridian.smolensk;
using meridian.smolensk.system;

namespace meridian.smolensk.protoStore
{
	public partial class companies_kind_activitiesStore : Meridian.IEntityStore, admin.db.IDataService<proto.companies_kind_activities>
	{
		public companies_kind_activitiesStore()
		{
			m_Items = new SortedList<long, proto.companies_kind_activities>();
		}
		private SortedList<long, proto.companies_kind_activities> m_Items;
		public void Commit()
		{
			//throw new NotImplementedException();
		}
		public IQueryable<proto.companies_kind_activities> GetAll()
		{
			return Queryable.AsQueryable<proto.companies_kind_activities>(All());
		}
		public proto.companies_kind_activities GetById(long id)
		{
			return Get(id);
		}
		void admin.db.IDataService<proto.companies_kind_activities>.Insert(proto.companies_kind_activities item)
		{
			Insert(item);
		}
		void admin.db.IDataService<proto.companies_kind_activities>.Delete(proto.companies_kind_activities item)
		{
			Delete(item);
		}
		public proto.companies_kind_activities CreateItem()
		{
			return new proto.companies_kind_activities() {   };
		}
		public void AbortItem(proto.companies_kind_activities item)
		{
			Delete(item);
		}
		public void Discard()
		{
			;
		}
		void admin.db.IDataService<proto.companies_kind_activities>.Update(proto.companies_kind_activities item)
		{
			Persist(item);
		}
		public object GetObject(long _id)
		{
			return Get(_id);
		}
		public System.Type GetObjectType()
		{
			return typeof(proto.companies_kind_activities);
		}
		public void DeleteById(long _id)
		{
			Delete(Get(_id));
		}
		public void UpdateById(long _id)
		{
			Update(Get(_id));
		}
		public void LoadAggregations(Meridian _meridian)
		{
			foreach(var item in m_Items.Values)
			{
				item.LoadAggregations(_meridian);
			}
		}
		public void Select(MySqlConnection _connection)
		{
			var cmd = new MySqlCommand("SELECT `id`, `company_id`, `kind_activitiy_id` FROM companies_kind_activities");
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.companies_kind_activities();
					item.LoadFromReader(reader);
					m_Items[item.id] = item;
				}
			}
		}
		public proto.companies_kind_activities Insert(MySqlConnection _connection, proto.companies_kind_activities _item)
		{
			var cmd = new MySqlCommand("INSERT INTO companies_kind_activities ( `company_id`, `kind_activitiy_id` ) VALUES ( @company_id, @kind_activitiy_id ); SELECT LAST_INSERT_ID();"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "company_id", Value = _item.company_id });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "kind_activitiy_id", Value = _item.kind_activitiy_id });
			cmd.Connection = _connection;
			_item.id = Convert.ToInt64(cmd.ExecuteScalar());
			m_Items.Add(_item.id, _item);
			_item.LoadAggregations(Meridian.Default);
			return _item;
		}
		public proto.companies_kind_activities Update(MySqlConnection _connection, proto.companies_kind_activities _item)
		{
			bool changed =  false;
			var cmdText =  "";
			var cmd = new MySqlCommand("UPDATE companies_kind_activities SET `company_id`= @company_id, `kind_activitiy_id`= @kind_activitiy_id WHERE id=@id"); ;
			if(_item.mc_id)
			{
			}
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			if(_item.mc_company_id)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "company_id = @company_id";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "company_id", Value = _item.company_id });
			}
			if(_item.mc_kind_activitiy_id)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "kind_activitiy_id = @kind_activitiy_id";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "kind_activitiy_id", Value = _item.kind_activitiy_id });
			}
			if(changed)
			{
				cmd.CommandText =  "UPDATE companies_kind_activities SET " + cmdText + " WHERE id=@id";
				cmd.Connection = _connection;
				cmd.ExecuteNonQuery();
				_item.LoadAggregations(Meridian.Default);
			}
			return _item;
		}
		public proto.companies_kind_activities Delete(MySqlConnection _connection, proto.companies_kind_activities _item)
		{
			var cmd = new MySqlCommand("DELETE FROM companies_kind_activities WHERE id=@id"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			cmd.Connection = _connection;
			cmd.ExecuteScalar();
			return _item;
		}
		public proto.companies_kind_activities Insert(proto.companies_kind_activities _item)
		{
			MeridianMonitor.Default.MySqlActionForeground((conn) => Insert(conn, _item));;
			return _item;
		}
		public proto.companies_kind_activities Persist(proto.companies_kind_activities _item)
		{
			if(_item.id <= 0)
			{
				_item = Insert(_item);
			}
			_item = Update(_item);
			return _item;
		}
		public proto.companies_kind_activities Delete(proto.companies_kind_activities _item)
		{
			_item.DeleteCompositions(Meridian.Default);
			_item.DeleteAggregations();
			m_Items.Remove(_item.id);
			MeridianMonitor.Default.MySqlActionBackground((conn) => Delete(conn, _item));;
			return _item;
		}
		public proto.companies_kind_activities Update(proto.companies_kind_activities _item)
		{
			MeridianMonitor.Default.MySqlActionBackground((conn) => Update(conn, _item));;
			return _item;
		}
		public IList<proto.companies_kind_activities> All()
		{
			return m_Items.Values;
		}
		public proto.companies_kind_activities Get(long _id)
		{
			return m_Items[_id];
		}
		public bool Exists(long _id)
		{
			return m_Items.ContainsKey(_id);
		}
		public void UpdateSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			var cmd = new MySqlCommand("SELECT `id`, `company_id`, `kind_activitiy_id` FROM companies_kind_activities WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					item.DeleteCompositions(Meridian.Default);
					item.DeleteAggregations();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
				}
			}
		}
		public void InsertSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if(Exists(_id)) return;;
			var cmd = new MySqlCommand("SELECT `id`, `company_id`, `kind_activitiy_id` FROM companies_kind_activities WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.companies_kind_activities();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
					m_Items.Add(item.id, item);
				}
			}
		}
		public void DeleteSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			item.DeleteCompositions(Meridian.Default);
			item.DeleteAggregations();
			m_Items.Remove(item.id);
		}
	}
}
