/* Automatically generated codefile, Meridian
 * Generated by magic, please do not interfere
 * Please sleep well and do not smoke. Love, Sam */

using System;
using System.Linq;
using System.Text;
using System.Data.Linq;
using System.Collections.Generic;
using MySql.Data.MySqlClient;
using meridian.smolensk;
using meridian.smolensk.system;

namespace meridian.smolensk.protoStore
{
	public partial class city_pricesStore : Meridian.IEntityStore, admin.db.IDataService<proto.city_prices>
	{
		public city_pricesStore()
		{
			m_Items = new SortedList<long, proto.city_prices>();
		}
		private SortedList<long, proto.city_prices> m_Items;
		public void Commit()
		{
			//throw new NotImplementedException();
		}
		public IQueryable<proto.city_prices> GetAll()
		{
			return Queryable.AsQueryable<proto.city_prices>(All());
		}
		public proto.city_prices GetById(long id)
		{
			return Get(id);
		}
		void admin.db.IDataService<proto.city_prices>.Insert(proto.city_prices item)
		{
			Insert(item);
		}
		void admin.db.IDataService<proto.city_prices>.Delete(proto.city_prices item)
		{
			Delete(item);
		}
		public proto.city_prices CreateItem()
		{
			return new proto.city_prices() {   };
		}
		public void AbortItem(proto.city_prices item)
		{
			Delete(item);
		}
		public void Discard()
		{
			;
		}
		void admin.db.IDataService<proto.city_prices>.Update(proto.city_prices item)
		{
			Persist(item);
		}
		public object GetObject(long _id)
		{
			return Get(_id);
		}
		public System.Type GetObjectType()
		{
			return typeof(proto.city_prices);
		}
		public void DeleteById(long _id)
		{
			Delete(Get(_id));
		}
		public void UpdateById(long _id)
		{
			Update(Get(_id));
		}
		public void LoadAggregations(Meridian _meridian)
		{
			foreach(var item in m_Items.Values)
			{
				item.LoadAggregations(_meridian);
			}
		}
		public void Select(MySqlConnection _connection)
		{
			var cmd = new MySqlCommand("SELECT `id`, `title`, `icon_id`, `value`, `html`, `url_icon`, `url_showall` FROM city_prices");
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.city_prices();
					item.LoadFromReader(reader);
					m_Items[item.id] = item;
				}
			}
		}
		public proto.city_prices Insert(MySqlConnection _connection, proto.city_prices _item)
		{
			var cmd = new MySqlCommand("INSERT INTO city_prices ( `title`, `icon_id`, `value`, `html`, `url_icon`, `url_showall` ) VALUES ( @title, @icon_id, @value, @html, @url_icon, @url_showall ); SELECT LAST_INSERT_ID();"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "icon_id", Value = _item.icon_id });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "value", Value = _item.value });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "html", Value = _item.html });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "url_icon", Value = _item.url_icon });
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "url_showall", Value = _item.url_showall });
			cmd.Connection = _connection;
			_item.id = Convert.ToInt64(cmd.ExecuteScalar());
			m_Items.Add(_item.id, _item);
			_item.LoadAggregations(Meridian.Default);
			return _item;
		}
		public proto.city_prices Update(MySqlConnection _connection, proto.city_prices _item)
		{
			bool changed =  false;
			var cmdText =  "";
			var cmd = new MySqlCommand("UPDATE city_prices SET `title`= @title, `icon_id`= @icon_id, `value`= @value, `html`= @html, `url_icon`= @url_icon, `url_showall`= @url_showall WHERE id=@id"); ;
			if(_item.mc_id)
			{
			}
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			if(_item.mc_title)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "title = @title";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "title", Value = _item.title != null ? (object)_item.title : DBNull.Value });
			}
			if(_item.mc_icon_id)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "icon_id = @icon_id";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "icon_id", Value = _item.icon_id });
			}
			if(_item.mc_value)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "value = @value";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "value", Value = _item.value != null ? (object)_item.value : DBNull.Value });
			}
			if(_item.mc_html)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "html = @html";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "html", Value = _item.html != null ? (object)_item.html : DBNull.Value });
			}
			if(_item.mc_url_icon)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "url_icon = @url_icon";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "url_icon", Value = _item.url_icon != null ? (object)_item.url_icon : DBNull.Value });
			}
			if(_item.mc_url_showall)
			{
				changed =  true;
				cmdText += (cmdText.Length > 0 ? ", " : "") + "url_showall = @url_showall";
				cmd.Parameters.Add( new MySqlParameter() { ParameterName = "url_showall", Value = _item.url_showall != null ? (object)_item.url_showall : DBNull.Value });
			}
			if(changed)
			{
				cmd.CommandText =  "UPDATE city_prices SET " + cmdText + " WHERE id=@id";
				cmd.Connection = _connection;
				cmd.ExecuteNonQuery();
				_item.LoadAggregations(Meridian.Default);
			}
			return _item;
		}
		public proto.city_prices Delete(MySqlConnection _connection, proto.city_prices _item)
		{
			var cmd = new MySqlCommand("DELETE FROM city_prices WHERE id=@id"); ;
			cmd.Parameters.Add( new MySqlParameter() { ParameterName = "id", Value = _item.id });
			cmd.Connection = _connection;
			cmd.ExecuteScalar();
			return _item;
		}
		public proto.city_prices Insert(proto.city_prices _item)
		{
			MeridianMonitor.Default.MySqlActionForeground((conn) => Insert(conn, _item));;
			return _item;
		}
		public proto.city_prices Persist(proto.city_prices _item)
		{
			if(_item.id <= 0)
			{
				_item = Insert(_item);
			}
			_item = Update(_item);
			return _item;
		}
		public proto.city_prices Delete(proto.city_prices _item)
		{
			_item.DeleteCompositions(Meridian.Default);
			_item.DeleteAggregations();
			m_Items.Remove(_item.id);
			MeridianMonitor.Default.MySqlActionBackground((conn) => Delete(conn, _item));;
			return _item;
		}
		public proto.city_prices Update(proto.city_prices _item)
		{
			MeridianMonitor.Default.MySqlActionBackground((conn) => Update(conn, _item));;
			return _item;
		}
		public IList<proto.city_prices> All()
		{
			return m_Items.Values;
		}
		public proto.city_prices Get(long _id)
		{
			return m_Items[_id];
		}
		public bool Exists(long _id)
		{
			return m_Items.ContainsKey(_id);
		}
		public void UpdateSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			var cmd = new MySqlCommand("SELECT `id`, `title`, `icon_id`, `value`, `html`, `url_icon`, `url_showall` FROM city_prices WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					item.DeleteCompositions(Meridian.Default);
					item.DeleteAggregations();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
				}
			}
		}
		public void InsertSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if(Exists(_id)) return;;
			var cmd = new MySqlCommand("SELECT `id`, `title`, `icon_id`, `value`, `html`, `url_icon`, `url_showall` FROM city_prices WHERE id = " + _id.ToString());
			cmd.Connection = _connection;
			using(var reader = cmd.ExecuteReader())
			{
				while(reader.Read())
				{
					var item = new proto.city_prices();
					item.LoadFromReader(reader);
					item.LoadAggregations(_meridian);
					item.LoadCompositions(_meridian);
					m_Items.Add(item.id, item);
				}
			}
		}
		public void DeleteSync(MySqlConnection _connection, long _id, Meridian _meridian)
		{
			if (!Exists(_id))
			{
			return;
			}
			var item = Get(_id);
			item.DeleteCompositions(Meridian.Default);
			item.DeleteAggregations();
			m_Items.Remove(item.id);
		}
	}
}
